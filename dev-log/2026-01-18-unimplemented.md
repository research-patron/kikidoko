# 未実装項目整理（2026-01-18）

## フロントエンド
- Firestoreのサーバー側ページネーション（現状は全件取得 + クライアントフィルタ + 8件表示）。
- 詳細ページ（`/equipment/:id`）とGoogle Mapsリンク表示。
- ページネーション（または無限スクロール）の導入。

## データ収集・正規化
- 住所の補完（大学住所データやジオコーディング）による精度向上。
- 条件/料金/外部利用可否の追加抽出（各ソースの詳細ページ対応）。
- 削除済み機器の追跡・非表示/削除の運用。
- 重複マージ戦略の精緻化（上書き優先順位の定義）。
- JS必須ポータル（東北大・NIMS・東工大など）の取得可否調査と、必要ならヘッドレス対応。
- 研究機関/大学の残りサイト（PDFのみ・ログイン必須を除外）を優先度順に追加。

## 運用・デプロイ
- クローラの定期実行（Cloud Run + Cloud Scheduler 等）設定。
- Firebase Hosting へのデプロイ（本番/ステージング）。
- Firestore セキュリティルールの設定（読み取り公開/書き込み制限）。

## 品質・テスト
- クローラの回帰テスト（HTML構造変更時の検知）。
- UI/検索条件の機能テストとレスポンシブ確認。
